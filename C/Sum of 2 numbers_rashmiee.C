#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b, c;

   printf("Enter two numbers to add\n");
   scanf("%d%d", &a, &b);

   c = b + a;

   printf("Sum of the numbers = %d\n", c);

   return 0;
}
